name: AI-transcript canonical parity (legacy manual wrapper)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Automatic validation moved to .github/workflows/ci.yml and .ci/run-ci-request."
          exit 1
